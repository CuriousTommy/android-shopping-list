<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.assignment6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <provider
            android:name=".data.ShoppingContentProvider"
            android:authorities="com.assignment6.shopping"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".ui.fragment.start.StartShoppingSessionFragment" />
        <activity android:name=".ui.fragment.start.StartShoppingSessionWithSpinnerFragment" />
        <activity android:name=".ui.fragment.start.StartManageFragment" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>